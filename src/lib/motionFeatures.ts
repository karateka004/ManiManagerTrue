/**
 * Набор фич framer-motion для LazyMotion (domMax — нужен из-за layoutId).
 * Грузится отдельным async-чанком ПОСЛЕ первого рендера, чтобы не раздувать
 * стартовый бандл движком анимаций. До загрузки `m.*` ведут себя как обычные
 * элементы (без анимации) — для мини-аппа это незаметно.
 */
export { domMax as default } from 'framer-motion'
