node_modules
dist
dist-ssr
.DS_Store
*.local
.vite
.env
.env.local
.idea
.vscode/*
!.vscode/settings.json

# Cloudflare tunnel (локальная разработка)
cloudflared.exe
tunnel.log

# Cloudflare Worker
worker/node_modules
worker/.wrangler
.dev.vars

# Исходники купленных/скачанных ассет-паков (CraftPix).
# Лицензия разрешает использовать графику в игре, но запрещает перераспространять
# сами файлы пака. В репозиторий уезжают только нарезанные спрайты (src/assets/game).
assets-raw

# Секреты — никогда не коммитим
*_secrets.json
worker/_secrets.json
.env.*.local
*.pem
*.key
